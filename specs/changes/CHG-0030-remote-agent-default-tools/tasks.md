---
kind: task-delta
id: CHG-0030-TASKS
status: complete
---

- [x] `TASK-001` 增加 Web Search/Web Fetch/JSON/RSS manifest、工具参数 Schema 和 first-party registry 注册。
- [x] `TASK-002` 实现公网 URL 校验、重定向复核、超时/字节/字符限制和不可信内容标记。
- [x] `TASK-003` 将六个只读工具接入新建 Agent 的 API/UI 默认挂载，保留显式空列表退出路径。
- [x] `TASK-004` 将前后端 Agent policy 与新子挂载并发默认值统一到可直接使用的范围。
- [x] `TASK-005` 补齐单元/API/源合同测试、浏览器按钮 smoke、全量门禁和公开发布记录；浏览器证据见 `docs/testing/manual-browser-2026-08-02.md`。
